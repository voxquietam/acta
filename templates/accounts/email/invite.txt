You've been invited to join the "{{ workspace.name }}" workspace on Acta.

Click the link below to create your account and join the workspace as {{ role_label }}:

{{ absolute_url }}

The link expires on {{ invite.expires_at|date:"M j, Y H:i" }} and can only be used once.

If you weren't expecting this, just ignore the message.

— Acta
